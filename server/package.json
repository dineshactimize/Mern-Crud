{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "description": "mern crud",
  "main": " ",
  
 "scripts": {
  "start": "npx nodemon server.js",
  "dev": "npx nodemon server.js",
  "build-frontend": "cd ../client && npm install && npm run build",
  "install-backend": "npm install ",
    "deploy": "npm run install-backend && npm run build-frontend"
},

  "author": "Dinesh",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^2.2.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "mongodb": "^7.0.0",
    "mongoose": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
